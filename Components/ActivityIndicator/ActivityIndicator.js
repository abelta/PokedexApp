import React from 'react';
import { ActivityIndicator } from 'react-native';

const ActivityIndicatorCustom = props => (
  <ActivityIndicator size="large" color="lightgrey" {...props} />
);

export default ActivityIndicatorCustom;
