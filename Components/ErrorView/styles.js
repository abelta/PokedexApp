import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  errorView: {
    padding: 20,
  },
  text: {
    fontSize: 20,
    textAlign: 'center',
  },
});

export default styles;
