import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  typesStats: {
    flexDirection: 'row',
    padding: 2,
  },
  bullet: {
    margin: 2,
  },
});

export default styles;
