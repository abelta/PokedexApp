import React from 'react';
import { Text } from 'react-native';
import { connect } from 'react-redux';

const PokemonDetail = () => <Text>POKEMON DETAIL</Text>;

export default connect()(PokemonDetail);
